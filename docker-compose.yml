version: '3.9'

services:
  producscan:
    image: iotdepartment/producscan:latest
    container_name: producscan
    ports:
      - "10000:8080"   # Expones el puerto 8080 del contenedor en el 10000 del servidor
    environment:
      - ConnectionStrings__DefaultConnection=Server=10.195.10.166,1433;Database=ScanSystemDb_Copy;User Id=Produccionn;Password=2025sqlserver!;TrustServerCertificate=True;
    restart: always